import {
  Animate,
  Ripple,
  Sidenav,
  Collapse,
  Dropdown,
  Tooltip,
  Button,
  Select,
  initTE,
} from "tw-elements";

initTE({
  // Animate,
  Ripple,

  // Sidenav,
  Collapse,
  Dropdown,
  // Tooltip,

  // Button,
  Select,
});
