import {
  Animate,
  Ripple,
  Tab,
  Sidenav,
  Collapse,
  Dropdown,
  Tooltip,
  Button,
  Select,
  initTE,
} from "tw-elements";

initTE({
  // Animate,
  Ripple,

  Tab,
  // Sidenav,
  Collapse,
  Dropdown,
  // Tooltip,

  // Button,
  Select,
});
